# Database Schema (live snapshot)

> Source: Supabase project `zpciertrkqwzuuektzpj`. Generated by `scripts/introspect-schema.mjs` at 2026-06-27T10:32:57.772Z.
> Do **not** hand-edit this file. Re-run `npm run supabase:introspect` after schema changes.

Scope: every `amb_*` table, view, storage bucket, RLS policy, and index in the `public` schema.

## Tables

- [`amb_activities`](#amb_activities)
- [`amb_chat_messages`](#amb_chat_messages)
- [`amb_events`](#amb_events)
- [`amb_gallery`](#amb_gallery)
- [`amb_orders`](#amb_orders)
- [`amb_points_ledger`](#amb_points_ledger)
- [`amb_products`](#amb_products)
- [`amb_profiles`](#amb_profiles)
- [`amb_submission_files`](#amb_submission_files)
- [`amb_submissions`](#amb_submissions)
- [`amb_tiers`](#amb_tiers)
- [`amb_v_leaderboard`](#amb_v_leaderboard)
- [`amb_v_user_balances`](#amb_v_user_balances)
- [`yuvaah_quiz_attempts`](#yuvaah_quiz_attempts)
- [`yuvaah_quiz_questions`](#yuvaah_quiz_questions)
- [`yuvaah_quiz_rounds`](#yuvaah_quiz_rounds)

### `amb_activities`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | title | text | NO |  |
| 3 | description | text | NO |  |
| 4 | points | int4 | NO |  |
| 5 | submission_deadline | timestamptz | NO |  |
| 6 | cover_image_url | text | YES |  |
| 7 | is_active | bool | NO | true |
| 8 | created_by | uuid | NO |  |
| 9 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_activities_points_check | CHECK | CHECK ((points >= 0)) |
| amb_activities_created_by_fkey | FOREIGN KEY | FOREIGN KEY (created_by) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_activities_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_activities_created_at_idx | CREATE INDEX amb_activities_created_at_idx ON public.amb_activities USING btree (created_at DESC) |
| amb_activities_is_active_idx | CREATE INDEX amb_activities_is_active_idx ON public.amb_activities USING btree (is_active) |
| amb_activities_pkey | CREATE UNIQUE INDEX amb_activities_pkey ON public.amb_activities USING btree (id) |
| amb_activities_submission_deadline_idx | CREATE INDEX amb_activities_submission_deadline_idx ON public.amb_activities USING btree (submission_deadline) |

**RLS Policies**

_(none)_

### `amb_chat_messages`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | sender_id | uuid | NO |  |
| 3 | receiver_id | uuid | NO |  |
| 4 | body | text | NO |  |
| 5 | read_at | timestamptz | YES |  |
| 6 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_chat_messages_body_check | CHECK | CHECK ((char_length(TRIM(BOTH FROM body)) > 0)) |
| amb_chat_messages_no_self | CHECK | CHECK ((sender_id <> receiver_id)) |
| amb_chat_messages_receiver_id_fkey | FOREIGN KEY | FOREIGN KEY (receiver_id) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_chat_messages_sender_id_fkey | FOREIGN KEY | FOREIGN KEY (sender_id) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_chat_messages_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_chat_messages_pair_idx | CREATE INDEX amb_chat_messages_pair_idx ON public.amb_chat_messages USING btree (sender_id, receiver_id, created_at) |
| amb_chat_messages_pkey | CREATE UNIQUE INDEX amb_chat_messages_pkey ON public.amb_chat_messages USING btree (id) |
| amb_chat_messages_receiver_created_idx | CREATE INDEX amb_chat_messages_receiver_created_idx ON public.amb_chat_messages USING btree (receiver_id, created_at DESC) |
| amb_chat_messages_unread_idx | CREATE INDEX amb_chat_messages_unread_idx ON public.amb_chat_messages USING btree (receiver_id, sender_id) WHERE (read_at IS NULL) |

**RLS Policies**

_(none)_

### `amb_events`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | title | text | NO |  |
| 3 | body | text | NO |  |
| 4 | cover_image_url | text | YES |  |
| 5 | created_by | uuid | NO |  |
| 6 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_events_created_by_fkey | FOREIGN KEY | FOREIGN KEY (created_by) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_events_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_events_created_at_idx | CREATE INDEX amb_events_created_at_idx ON public.amb_events USING btree (created_at DESC) |
| amb_events_pkey | CREATE UNIQUE INDEX amb_events_pkey ON public.amb_events USING btree (id) |

**RLS Policies**

_(none)_

### `amb_gallery`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | image_url | text | NO |  |
| 3 | caption | text | YES |  |
| 4 | created_by | uuid | NO |  |
| 5 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_gallery_created_by_fkey | FOREIGN KEY | FOREIGN KEY (created_by) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_gallery_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_gallery_created_at_idx | CREATE INDEX amb_gallery_created_at_idx ON public.amb_gallery USING btree (created_at DESC) |
| amb_gallery_pkey | CREATE UNIQUE INDEX amb_gallery_pkey ON public.amb_gallery USING btree (id) |

**RLS Policies**

_(none)_

### `amb_orders`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | user_id | uuid | NO |  |
| 3 | product_id | uuid | NO |  |
| 4 | points_used | int4 | NO |  |
| 5 | inr_paid | numeric | NO | 0 |
| 6 | payment_status | text | NO |  |
| 7 | payment_ref | text | YES |  |
| 8 | fulfillment_status | text | NO | 'pending'::text |
| 9 | admin_notes | text | YES |  |
| 10 | created_at | timestamptz | NO | now() |
| 11 | razorpay_order_id | text | YES |  |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_orders_fulfillment_status_check | CHECK | CHECK ((fulfillment_status = ANY (ARRAY['pending'::text, 'fulfilled'::text, 'cancelled'::text]))) |
| amb_orders_inr_paid_check | CHECK | CHECK ((inr_paid >= (0)::numeric)) |
| amb_orders_payment_status_check | CHECK | CHECK ((payment_status = ANY (ARRAY['not_required'::text, 'pending'::text, 'paid'::text, 'failed'::text]))) |
| amb_orders_points_used_check | CHECK | CHECK ((points_used >= 0)) |
| amb_orders_product_id_fkey | FOREIGN KEY | FOREIGN KEY (product_id) REFERENCES amb_products(id) ON DELETE RESTRICT |
| amb_orders_user_id_fkey | FOREIGN KEY | FOREIGN KEY (user_id) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_orders_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_orders_created_at_idx | CREATE INDEX amb_orders_created_at_idx ON public.amb_orders USING btree (created_at DESC) |
| amb_orders_fulfillment_status_idx | CREATE INDEX amb_orders_fulfillment_status_idx ON public.amb_orders USING btree (fulfillment_status) |
| amb_orders_payment_status_idx | CREATE INDEX amb_orders_payment_status_idx ON public.amb_orders USING btree (payment_status) |
| amb_orders_pkey | CREATE UNIQUE INDEX amb_orders_pkey ON public.amb_orders USING btree (id) |
| amb_orders_product_id_idx | CREATE INDEX amb_orders_product_id_idx ON public.amb_orders USING btree (product_id) |
| amb_orders_razorpay_order_id_idx | CREATE INDEX amb_orders_razorpay_order_id_idx ON public.amb_orders USING btree (razorpay_order_id) |
| amb_orders_user_id_idx | CREATE INDEX amb_orders_user_id_idx ON public.amb_orders USING btree (user_id) |

**RLS Policies**

_(none)_

### `amb_points_ledger`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | user_id | uuid | NO |  |
| 3 | delta | int4 | NO |  |
| 4 | reason | text | NO |  |
| 5 | reference_id | uuid | YES |  |
| 6 | note | text | YES |  |
| 7 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_points_ledger_reason_check | CHECK | CHECK ((reason = ANY (ARRAY['submission_awarded'::text, 'order_redemption'::text, 'admin_adjustment'::text, 'award_adjustment'::text, 'quiz_score'::text]))) |
| amb_points_ledger_user_id_fkey | FOREIGN KEY | FOREIGN KEY (user_id) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_points_ledger_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_points_ledger_created_at_idx | CREATE INDEX amb_points_ledger_created_at_idx ON public.amb_points_ledger USING btree (created_at DESC) |
| amb_points_ledger_pkey | CREATE UNIQUE INDEX amb_points_ledger_pkey ON public.amb_points_ledger USING btree (id) |
| amb_points_ledger_reference_id_idx | CREATE INDEX amb_points_ledger_reference_id_idx ON public.amb_points_ledger USING btree (reference_id) |
| amb_points_ledger_user_id_idx | CREATE INDEX amb_points_ledger_user_id_idx ON public.amb_points_ledger USING btree (user_id) |

**RLS Policies**

_(none)_

### `amb_products`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | type | text | NO |  |
| 3 | name | text | NO |  |
| 4 | description | text | NO | ''::text |
| 5 | image_url | text | YES |  |
| 6 | points_cost | int4 | NO |  |
| 7 | inr_cost | numeric | NO |  |
| 8 | stock | int4 | YES |  |
| 9 | is_active | bool | NO | true |
| 10 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_products_has_price | CHECK | CHECK (((points_cost > 0) OR (inr_cost > (0)::numeric))) |
| amb_products_inr_cost_check | CHECK | CHECK ((inr_cost >= (0)::numeric)) |
| amb_products_points_cost_check | CHECK | CHECK ((points_cost >= 0)) |
| amb_products_stock_check | CHECK | CHECK (((stock IS NULL) OR (stock >= 0))) |
| amb_products_type_check | CHECK | CHECK ((type = ANY (ARRAY['merchandise'::text, 'voucher'::text]))) |
| amb_products_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_products_created_at_idx | CREATE INDEX amb_products_created_at_idx ON public.amb_products USING btree (created_at DESC) |
| amb_products_is_active_idx | CREATE INDEX amb_products_is_active_idx ON public.amb_products USING btree (is_active) |
| amb_products_pkey | CREATE UNIQUE INDEX amb_products_pkey ON public.amb_products USING btree (id) |
| amb_products_type_idx | CREATE INDEX amb_products_type_idx ON public.amb_products USING btree (type) |

**RLS Policies**

_(none)_

### `amb_profiles`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | auth_user_id | uuid | YES |  |
| 3 | role | text | NO | 'ambassador'::text |
| 4 | status | text | NO | 'pending'::text |
| 5 | first_name | text | NO |  |
| 6 | last_name | text | NO |  |
| 7 | email | text | NO |  |
| 8 | phone | text | NO |  |
| 9 | college | text | NO |  |
| 10 | city | text | NO |  |
| 11 | student_id_url | text | YES |  |
| 12 | avatar_url | text | YES |  |
| 13 | application_data | jsonb | NO | '{}'::jsonb |
| 14 | created_at | timestamptz | NO | now() |
| 15 | approved_at | timestamptz | YES |  |
| 16 | rejected_at | timestamptz | YES |  |
| 17 | referral_code | text | YES |  |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_profiles_role_check | CHECK | CHECK ((role = ANY (ARRAY['admin'::text, 'ambassador'::text]))) |
| amb_profiles_status_check | CHECK | CHECK ((status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text, 'suspended'::text]))) |
| amb_profiles_auth_user_id_fkey | FOREIGN KEY | FOREIGN KEY (auth_user_id) REFERENCES auth.users(id) ON DELETE SET NULL |
| amb_profiles_pkey | PRIMARY KEY | PRIMARY KEY (id) |
| amb_profiles_email_unique | UNIQUE | UNIQUE (email) |

**Indexes**

| name | definition |
| --- | --- |
| amb_profiles_auth_user_id_idx | CREATE INDEX amb_profiles_auth_user_id_idx ON public.amb_profiles USING btree (auth_user_id) |
| amb_profiles_city_idx | CREATE INDEX amb_profiles_city_idx ON public.amb_profiles USING btree (city) |
| amb_profiles_college_idx | CREATE INDEX amb_profiles_college_idx ON public.amb_profiles USING btree (college) |
| amb_profiles_created_at_idx | CREATE INDEX amb_profiles_created_at_idx ON public.amb_profiles USING btree (created_at DESC) |
| amb_profiles_email_unique | CREATE UNIQUE INDEX amb_profiles_email_unique ON public.amb_profiles USING btree (email) |
| amb_profiles_pkey | CREATE UNIQUE INDEX amb_profiles_pkey ON public.amb_profiles USING btree (id) |
| amb_profiles_referral_code_unique_idx | CREATE UNIQUE INDEX amb_profiles_referral_code_unique_idx ON public.amb_profiles USING btree (upper(referral_code)) |
| amb_profiles_status_idx | CREATE INDEX amb_profiles_status_idx ON public.amb_profiles USING btree (status) |

**RLS Policies**

_(none)_

### `amb_submission_files`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | submission_id | uuid | NO |  |
| 3 | storage_path | text | NO |  |
| 4 | file_type | text | NO |  |
| 5 | file_size | int8 | NO |  |
| 6 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_submission_files_file_size_check | CHECK | CHECK ((file_size > 0)) |
| amb_submission_files_submission_id_fkey | FOREIGN KEY | FOREIGN KEY (submission_id) REFERENCES amb_submissions(id) ON DELETE CASCADE |
| amb_submission_files_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_submission_files_pkey | CREATE UNIQUE INDEX amb_submission_files_pkey ON public.amb_submission_files USING btree (id) |
| amb_submission_files_submission_id_idx | CREATE INDEX amb_submission_files_submission_id_idx ON public.amb_submission_files USING btree (submission_id) |

**RLS Policies**

_(none)_

### `amb_submissions`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | activity_id | uuid | NO |  |
| 3 | user_id | uuid | NO |  |
| 4 | text_content | text | YES |  |
| 5 | status | text | NO | 'submitted'::text |
| 6 | awarded_points | int4 | YES |  |
| 7 | reviewed_by | uuid | YES |  |
| 8 | reviewed_at | timestamptz | YES |  |
| 9 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_submissions_awarded_points_check | CHECK | CHECK (((awarded_points IS NULL) OR (awarded_points >= 0))) |
| amb_submissions_status_check | CHECK | CHECK ((status = ANY (ARRAY['submitted'::text, 'awarded'::text]))) |
| amb_submissions_activity_id_fkey | FOREIGN KEY | FOREIGN KEY (activity_id) REFERENCES amb_activities(id) ON DELETE RESTRICT |
| amb_submissions_reviewed_by_fkey | FOREIGN KEY | FOREIGN KEY (reviewed_by) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_submissions_user_id_fkey | FOREIGN KEY | FOREIGN KEY (user_id) REFERENCES amb_profiles(id) ON DELETE RESTRICT |
| amb_submissions_pkey | PRIMARY KEY | PRIMARY KEY (id) |
| amb_submissions_unique_per_user | UNIQUE | UNIQUE (activity_id, user_id) |

**Indexes**

| name | definition |
| --- | --- |
| amb_submissions_activity_id_idx | CREATE INDEX amb_submissions_activity_id_idx ON public.amb_submissions USING btree (activity_id) |
| amb_submissions_created_at_idx | CREATE INDEX amb_submissions_created_at_idx ON public.amb_submissions USING btree (created_at DESC) |
| amb_submissions_pkey | CREATE UNIQUE INDEX amb_submissions_pkey ON public.amb_submissions USING btree (id) |
| amb_submissions_status_idx | CREATE INDEX amb_submissions_status_idx ON public.amb_submissions USING btree (status) |
| amb_submissions_unique_per_user | CREATE UNIQUE INDEX amb_submissions_unique_per_user ON public.amb_submissions USING btree (activity_id, user_id) |
| amb_submissions_user_id_idx | CREATE INDEX amb_submissions_user_id_idx ON public.amb_submissions USING btree (user_id) |

**RLS Policies**

_(none)_

### `amb_tiers`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | rank | int4 | NO |  |
| 2 | name | text | NO |  |
| 3 | threshold_points | int4 | NO |  |
| 4 | points_to_inr_rate | numeric | NO |  |
| 5 | updated_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| amb_tiers_name_check | CHECK | CHECK (((length(name) >= 1) AND (length(name) <= 64))) |
| amb_tiers_points_to_inr_rate_check | CHECK | CHECK ((points_to_inr_rate > (0)::numeric)) |
| amb_tiers_rank_check | CHECK | CHECK (((rank >= 1) AND (rank <= 5))) |
| amb_tiers_threshold_points_check | CHECK | CHECK ((threshold_points >= 0)) |
| amb_tiers_pkey | PRIMARY KEY | PRIMARY KEY (rank) |

**Indexes**

| name | definition |
| --- | --- |
| amb_tiers_pkey | CREATE UNIQUE INDEX amb_tiers_pkey ON public.amb_tiers USING btree (rank) |

**RLS Policies**

_(none)_

### `amb_v_leaderboard`

**RLS enabled:** **NO**

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | user_id | uuid | YES |  |
| 2 | first_name | text | YES |  |
| 3 | last_name | text | YES |  |
| 4 | total_earned | int4 | YES |  |

**Constraints**

_(none)_

**Indexes**

_(none)_

**RLS Policies**

_(none)_

### `amb_v_user_balances`

**RLS enabled:** **NO**

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | user_id | uuid | YES |  |
| 2 | balance | int4 | YES |  |

**Constraints**

_(none)_

**Indexes**

_(none)_

**RLS Policies**

_(none)_

### `yuvaah_quiz_attempts`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | round_id | uuid | NO |  |
| 3 | profile_id | uuid | NO |  |
| 4 | status | text | NO | 'in_progress'::text |
| 5 | assigned_question_ids | jsonb | NO |  |
| 6 | answers | jsonb | NO | '{}'::jsonb |
| 7 | score | int4 | YES |  |
| 8 | correct_count | int4 | YES |  |
| 9 | wrong_count | int4 | YES |  |
| 10 | unanswered_count | int4 | YES |  |
| 11 | started_at | timestamptz | NO | now() |
| 12 | completed_at | timestamptz | YES |  |
| 13 | points_ledger_id | uuid | YES |  |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| yuvaah_quiz_attempts_status_check | CHECK | CHECK ((status = ANY (ARRAY['in_progress'::text, 'completed'::text]))) |
| yuvaah_quiz_attempts_points_ledger_id_fkey | FOREIGN KEY | FOREIGN KEY (points_ledger_id) REFERENCES amb_points_ledger(id) ON DELETE SET NULL |
| yuvaah_quiz_attempts_profile_id_fkey | FOREIGN KEY | FOREIGN KEY (profile_id) REFERENCES amb_profiles(id) ON DELETE CASCADE |
| yuvaah_quiz_attempts_round_id_fkey | FOREIGN KEY | FOREIGN KEY (round_id) REFERENCES yuvaah_quiz_rounds(id) ON DELETE CASCADE |
| yuvaah_quiz_attempts_pkey | PRIMARY KEY | PRIMARY KEY (id) |
| yuvaah_quiz_attempts_round_id_profile_id_key | UNIQUE | UNIQUE (round_id, profile_id) |

**Indexes**

| name | definition |
| --- | --- |
| yuvaah_quiz_attempts_pkey | CREATE UNIQUE INDEX yuvaah_quiz_attempts_pkey ON public.yuvaah_quiz_attempts USING btree (id) |
| yuvaah_quiz_attempts_profile_idx | CREATE INDEX yuvaah_quiz_attempts_profile_idx ON public.yuvaah_quiz_attempts USING btree (profile_id) |
| yuvaah_quiz_attempts_round_id_profile_id_key | CREATE UNIQUE INDEX yuvaah_quiz_attempts_round_id_profile_id_key ON public.yuvaah_quiz_attempts USING btree (round_id, profile_id) |
| yuvaah_quiz_attempts_round_idx | CREATE INDEX yuvaah_quiz_attempts_round_idx ON public.yuvaah_quiz_attempts USING btree (round_id) |

**RLS Policies**

_(none)_

### `yuvaah_quiz_questions`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | round_id | uuid | NO |  |
| 3 | category | text | YES |  |
| 4 | question | text | NO |  |
| 5 | option_a | text | NO |  |
| 6 | option_b | text | NO |  |
| 7 | option_c | text | NO |  |
| 8 | option_d | text | NO |  |
| 9 | correct_index | int2 | NO |  |
| 10 | created_at | timestamptz | NO | now() |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| yuvaah_quiz_questions_correct_index_check | CHECK | CHECK (((correct_index >= 0) AND (correct_index <= 3))) |
| yuvaah_quiz_questions_round_id_fkey | FOREIGN KEY | FOREIGN KEY (round_id) REFERENCES yuvaah_quiz_rounds(id) ON DELETE CASCADE |
| yuvaah_quiz_questions_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| yuvaah_quiz_questions_pkey | CREATE UNIQUE INDEX yuvaah_quiz_questions_pkey ON public.yuvaah_quiz_questions USING btree (id) |
| yuvaah_quiz_questions_round_idx | CREATE INDEX yuvaah_quiz_questions_round_idx ON public.yuvaah_quiz_questions USING btree (round_id) |

**RLS Policies**

_(none)_

### `yuvaah_quiz_rounds`

**RLS enabled:** yes

**Columns**

| # | column | type | nullable | default |
| --- | --- | --- | --- | --- |
| 1 | id | uuid | NO | gen_random_uuid() |
| 2 | title | text | NO |  |
| 3 | description | text | YES |  |
| 4 | status | text | NO | 'draft'::text |
| 5 | time_limit_seconds | int4 | YES |  |
| 6 | points_per_correct | int4 | NO | 10 |
| 7 | questions_per_attempt | int4 | NO | 10 |
| 8 | created_by | uuid | YES |  |
| 9 | created_at | timestamptz | NO | now() |
| 10 | activated_at | timestamptz | YES |  |
| 11 | closed_at | timestamptz | YES |  |

**Constraints**

| name | type | definition |
| --- | --- | --- |
| yuvaah_quiz_rounds_points_per_correct_check | CHECK | CHECK ((points_per_correct >= 0)) |
| yuvaah_quiz_rounds_questions_per_attempt_check | CHECK | CHECK ((questions_per_attempt > 0)) |
| yuvaah_quiz_rounds_status_check | CHECK | CHECK ((status = ANY (ARRAY['draft'::text, 'active'::text, 'closed'::text]))) |
| yuvaah_quiz_rounds_time_limit_seconds_check | CHECK | CHECK (((time_limit_seconds IS NULL) OR (time_limit_seconds >= 0))) |
| yuvaah_quiz_rounds_created_by_fkey | FOREIGN KEY | FOREIGN KEY (created_by) REFERENCES amb_profiles(id) ON DELETE SET NULL |
| yuvaah_quiz_rounds_pkey | PRIMARY KEY | PRIMARY KEY (id) |

**Indexes**

| name | definition |
| --- | --- |
| yuvaah_quiz_rounds_one_active_idx | CREATE UNIQUE INDEX yuvaah_quiz_rounds_one_active_idx ON public.yuvaah_quiz_rounds USING btree (status) WHERE (status = 'active'::text) |
| yuvaah_quiz_rounds_pkey | CREATE UNIQUE INDEX yuvaah_quiz_rounds_pkey ON public.yuvaah_quiz_rounds USING btree (id) |
| yuvaah_quiz_rounds_status_idx | CREATE INDEX yuvaah_quiz_rounds_status_idx ON public.yuvaah_quiz_rounds USING btree (status) |

**RLS Policies**

_(none)_

## Views

### `amb_v_leaderboard`

```sql
SELECT p.id AS user_id,
    p.first_name,
    p.last_name,
    (COALESCE(sum(l.delta), (0)::bigint))::integer AS total_earned
   FROM (amb_profiles p
     LEFT JOIN amb_points_ledger l ON (((l.user_id = p.id) AND (l.reason = ANY (ARRAY['submission_awarded'::text, 'award_adjustment'::text, 'quiz_score'::text])))))
  WHERE ((p.role = 'ambassador'::text) AND (p.status = 'approved'::text))
  GROUP BY p.id, p.first_name, p.last_name
  ORDER BY ((COALESCE(sum(l.delta), (0)::bigint))::integer) DESC;
```

### `amb_v_user_balances`

```sql
SELECT user_id,
    (COALESCE(sum(delta), (0)::bigint))::integer AS balance
   FROM amb_points_ledger
  GROUP BY user_id;
```

## Storage Buckets (`amb_*`)

| name | public | size_limit_bytes | allowed_mime | created_at |
| --- | --- | --- | --- | --- |
| amb_activities | true | 5242880 | image/jpeg, image/png, image/webp | 2026-04-25 08:02:37.316014+00 |
| amb_applications | false |  |  | 2026-04-24 11:31:26.130799+00 |
| amb_events | true | 5242880 | image/jpeg, image/png, image/webp | 2026-04-25 09:36:18.960426+00 |
| amb_gallery | true | 5242880 | image/jpeg, image/png, image/webp | 2026-04-25 09:36:18.960426+00 |
| amb_products | true | 5242880 | image/jpeg, image/png, image/webp | 2026-04-25 10:25:36.72574+00 |
| amb_submissions | false | 52428800 | image/jpeg, image/png, image/webp, image/gif, video/mp4, video/quicktime, video/webm, application/pdf, application/zip, application/x-zip-compressed | 2026-04-25 08:21:23.271044+00 |

## Findings & Gaps (auto-generated)

- `amb_activities`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_chat_messages`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_events`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_gallery`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_orders`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_points_ledger`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_products`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_profiles`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_submission_files`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_submissions`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `amb_tiers`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `yuvaah_quiz_attempts`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `yuvaah_quiz_questions`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- `yuvaah_quiz_rounds`: **RLS is enabled but there are 0 policies** — the table is therefore unreadable by `anon` and `authenticated` roles. Reads currently work only via the service-role key (RLS bypass). Add SELECT policies before the admin UI relies on session-scoped queries, or accept that the admin UI must use the service-role client.
- Bucket `amb_applications`: no `file_size_limit` set at the bucket level. Tech doc §7 caps Student ID at 5 MB — currently enforced application-side only. Acceptable for Phase 1; revisit before public scale.
- Bucket `amb_applications`: no `allowed_mime_types` set at the bucket level. Allowed types (`image/jpeg`, `image/png`, `image/webp`, `application/pdf`) are enforced application-side only.
